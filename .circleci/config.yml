version: 2
jobs:
  build:
    docker:
      - image: circleci/node:17.9-browsers
        environment:          
          DBUS_SESSION_BUS_ADDRESS: /dev/null
    steps:
      - checkout


      - run: google-chrome --version 69.0.3497.81
      
      - save_cache:
          key: node_modules-{{ .Branch }}-{{ checksum "package-lock.json" }}
          paths:
            - "node_modules"
            - "./node_modules"


      - run:
          name: NPM Install
          command: |
            npm install


      - run:
          name: Install Protractor
          command: |
            sudo npm i -g protractor


      - run:
          name: WebDriver Update
          command: | 
            ./node_modules/.bin/webdriver-manager update --versions.chrome 69.0.3497.81
            ./node_modules/.bin/webdriver-manager start --versions.chrome 69.0.3497.81  

      - run:
          name: WebDriver Update Selenium
          command: |      
            sudo webdriver-manager update
       


      - run: sleep 30s


      - run:
          name: Test Run
          command: |
            protractor configurationFile/RunnerConf.js